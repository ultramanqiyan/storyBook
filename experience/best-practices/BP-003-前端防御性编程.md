# BP-003 前端防御性编程

## 适用场景

前端JavaScript开发，处理DOM操作和API调用。

## 实践内容

### DOM操作空值检查

```javascript
// 正确做法
const element = document.getElementById('myElement');
if (element) {
  element.style.display = 'block';
}

// 或使用可选链
document.getElementById('myElement')?.classList.add('active');
```

### API调用错误处理

```javascript
async function fetchData() {
  try {
    const response = await fetch('/api/data');
    const result = await response.json();
    
    if (!result.success) {
      console.error('API Error:', result.error);
      showToast(result.error);
      return null;
    }
    
    return result.data;
  } catch (error) {
    console.error('Network Error:', error);
    showToast('网络错误，请重试');
    return null;
  }
}
```

### 数据兼容性处理

```javascript
// 处理可能不存在的字段
const name = data.original_name || data.character_name || '未知';

// 处理可能为空的数组
const items = data.items || [];

// 处理可能为空的对象
const user = data.user || {};
```

### 表单验证

```javascript
function validateForm(formData) {
  const errors = [];
  
  if (!formData.username || formData.username.trim() === '') {
    errors.push('用户名不能为空');
  }
  
  if (formData.username.length > 20) {
    errors.push('用户名不能超过20个字符');
  }
  
  return errors;
}
```

## 注意事项

1. 所有DOM操作前检查元素是否存在
2. 所有API调用使用try-catch
3. 处理所有可能的空值情况
4. 提供用户友好的错误提示

## 参考资料

- [JavaScript可选链操作符](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Operators/Optional_chaining)
